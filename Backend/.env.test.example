# Optional: omit this file to let the test runner start PostgreSQL with Docker.
# If supplied, the database name must contain a separate "test" segment.
TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/staysync_test
